@page
@model Lab12.Pages.Categories.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>
<h3>Are you sure you want to delete this category?</h3>
<hr />

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Category.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<div>
    <form style="width: 190px;" method="post" action="@Url.Action("Delete", new { id = Model.Category.Id })">
		@Html.AntiForgeryToken()
        <button type="submit" class="btn btn-danger btn-spacing margin-top" style="width: 150px;">Delete</button>
	</form>
</div>

<div>
    <a asp-page="/Categories/Index" class="btn btn-primary margin-top" style="width: 150px;">Back to List</a>
</div>